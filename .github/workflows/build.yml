name: Build Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: dtolnay/rust-toolchain@stable
      - name: Copy Windows ADB
        run: |
          mkdir -p src-tauri/resources/adb
          cp src-tauri/resources/adb-windows/* src-tauri/resources/adb/
        shell: bash
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run tauri build
      - uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: |
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: dtolnay/rust-toolchain@stable
      - name: Copy macOS ADB
        run: |
          mkdir -p src-tauri/resources/adb
          cp src-tauri/resources/adb-macos/* src-tauri/resources/adb/
          chmod +x src-tauri/resources/adb/adb
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run tauri build
      - uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: src-tauri/target/release/bundle/dmg/*.dmg
